apiVersion: apps/v1
kind: Deployment
metadata:
  name: kb-gateway
spec:
  template:
    spec:
      containers:
        - name: kb-gateway
          env:
            - name: EUREKA_ENABLED
              value: "true"
            - name: EUREKA_SERVER_URL
              value: http://kb-eureka-server:8761
